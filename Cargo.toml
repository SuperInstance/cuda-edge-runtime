[package]
name = "cuda-edge-runtime"
version = "0.2.0"
edition = "2021"
description = "Trust engine and fleet coordination — uses cuda-equipment"

[dependencies]
# cuda-equipment = { path = "../cuda-equipment" }
